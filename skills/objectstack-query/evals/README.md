# objectstack-query — Evals

Test cases for the objectstack-query skill.

## Planned Evals

1. **Simple filter** — "Filter accounts where status is active"
2. **Nested relation filter** — "Find orders where the customer's country is US"
3. **Aggregation** — "Count deals by region and show total revenue"
4. **Pagination** — "Implement cursor-based pagination for a list API"
5. **Full-text search** — "Search articles by keyword with fuzzy matching"
