{
  "name": "@objectstack/example-kernel",
  "version": "1.0.0",
  "description": "Comprehensive Kernel Protocol examples demonstrating Context, Logger, Manifests, Plugins, and Scoped Storage",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@objectstack/spec": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
