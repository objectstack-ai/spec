{
  "$ref": "#/definitions/DatasetMode",
  "definitions": {
    "DatasetMode": {
      "type": "string",
      "enum": [
        "insert",
        "update",
        "upsert",
        "replace",
        "ignore"
      ],
      "description": "Strategy for handling conflicts when importing dataset records"
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}