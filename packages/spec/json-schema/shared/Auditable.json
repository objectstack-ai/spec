{
  "$ref": "#/definitions/Auditable",
  "definitions": {
    "Auditable": {
      "type": "object",
      "properties": {
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp when the entity was created"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp when the entity was last updated"
        },
        "createdBy": {
          "type": "string",
          "description": "User ID or system identifier who created the entity"
        },
        "updatedBy": {
          "type": "string",
          "description": "User ID or system identifier who last updated the entity"
        }
      },
      "required": [
        "createdAt",
        "updatedAt",
        "createdBy",
        "updatedBy"
      ],
      "additionalProperties": false
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}