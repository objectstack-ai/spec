{
  "$ref": "#/definitions/Feature",
  "definitions": {
    "Feature": {
      "type": "object",
      "properties": {
        "code": {
          "type": "string",
          "pattern": "^[a-z_][a-z0-9_.]*$",
          "description": "Feature code (e.g. core.api_access)"
        },
        "label": {
          "type": "string",
          "description": "Human-readable feature name"
        },
        "description": {
          "type": "string",
          "description": "Description of the feature"
        },
        "type": {
          "type": "string",
          "enum": [
            "boolean",
            "counter",
            "gauge"
          ],
          "description": "Type of metric (boolean flag, counter, or gauge)",
          "default": "boolean"
        },
        "unit": {
          "type": "string",
          "enum": [
            "count",
            "bytes",
            "seconds",
            "percent"
          ],
          "description": "Unit of measurement for counter/gauge metrics"
        },
        "requires": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of prerequisite feature codes"
        }
      },
      "required": [
        "code",
        "label"
      ],
      "additionalProperties": false
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}