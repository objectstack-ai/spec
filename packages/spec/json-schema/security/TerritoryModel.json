{
  "$ref": "#/definitions/TerritoryModel",
  "definitions": {
    "TerritoryModel": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Model Name (e.g. FY24 Planning)"
        },
        "state": {
          "type": "string",
          "enum": [
            "planning",
            "active",
            "archived"
          ],
          "default": "planning",
          "description": "Current state of the territory model"
        },
        "startDate": {
          "type": "string",
          "description": "Effective start date for this model"
        },
        "endDate": {
          "type": "string",
          "description": "Effective end date for this model"
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}