{
  "$ref": "#/definitions/ListSpacesRequest",
  "definitions": {
    "ListSpacesRequest": {
      "type": "object",
      "properties": {
        "page": {
          "type": "integer",
          "minimum": 1,
          "default": 1,
          "description": "Page number (1-indexed)"
        },
        "perPage": {
          "type": "integer",
          "minimum": 1,
          "maximum": 100,
          "default": 20,
          "description": "Number of items per page"
        },
        "sortBy": {
          "type": "string",
          "description": "Field name to sort results by"
        },
        "sortOrder": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ],
          "default": "desc",
          "description": "Sort direction"
        },
        "ownerId": {
          "type": "string",
          "description": "Filter by owner"
        },
        "search": {
          "type": "string",
          "description": "Search in name and slug"
        }
      },
      "additionalProperties": false,
      "description": "Request parameters for listing spaces"
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}